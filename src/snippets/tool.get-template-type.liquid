{% comment %}
  Get Template Type
    Simple snippet that returns the type of template (removes the view) based on
    the template tag.

  Params:
    templateName (Optional) - Template name to extract the template type out of
                              Defaults to the current template.
  Returns:
    templateType - The template type (e.g. 'product' will be extracted from
                  'product.json')

  Version:
    1.0.0 - 2018/08/03
{% endcomment %}
{%- unless templateName != blank -%}
  {%- assign templateName = template -%}
{%- endunless -%}

{%- comment -%} Split by the . seperator {%- endcomment -%}
{%- assign templateSplit = templateName | split: '.' -%}
{%- assign templateType = templateSplit[0] -%}
