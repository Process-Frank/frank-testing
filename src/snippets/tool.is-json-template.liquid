{%- comment -%}
  Is JSON Template
    Simple tool to test if the current active template is a JSON Template, or a
    html/liquid template

  Returns:
    isJSON - true if a JSON template, otherwise false

  Version:
    1.0.0 - 2018/08/06
{%- endcomment -%}
{%- assign isJSON = false -%}
{%- if template contains 'json' -%}
  {%- assign isJSON = true -%}
{%- endif -%}
