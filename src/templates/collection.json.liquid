{%- comment -%}
  Collection JSON Template
    Contains all the needed information about a specified collection.

  Dependencies:
    2018/08/29

  Version:
    1.0.0 - 2018/08/29
{%- endcomment -%}
{%- layout none -%}

{%- comment -%}
  Since the collection MAY have a tag filter, let's just remove that by fetching
  the collection object again
{%- endcomment -%}
{%- assign coll = collections[collection.handle] -%}
{%- include 'tool.json-collection' with { collection: coll, includeProductData: true } -%}
